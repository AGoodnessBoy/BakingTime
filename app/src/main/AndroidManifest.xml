<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moming.jml.backingtime">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.moming.jml.bakingtime.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name="com.moming.jml.bakingtime.sync.RecipeSyncIntentService"
                 android:exported="false"
            ></service>
        <provider
            android:authorities="com.moming.jml.backingtime"
            android:name="com.moming.jml.bakingtime.data.RecipeProvider"
            android:exported="false"/>
    </application>


</manifest>